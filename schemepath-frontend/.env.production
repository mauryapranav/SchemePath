# Production environment — committed to repo (no secrets here)
# Real secrets are set in the Vercel dashboard → Project Settings → Environment Variables

# Point to your deployed Render backend URL
# Replace with your actual Render service URL after deployment
NEXT_PUBLIC_API_URL=https://schemepath-api.onrender.com
